#ifndef MEETINGUTILITIES_H
#define MEETINGUTILITIES_H

#include <string>

using namespace std;

void Swap(int* a, int* b);
int Partition(int array[], int low, int high);
void QuickSort(int array[], int low, int high);
void PrintArray(int array[], int size);

#endif