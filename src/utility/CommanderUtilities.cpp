#include <list>
#include <algorithm>
#include "../include/GraphUtilities.hpp"

using namespace std;

int GetCommander(Graph graph, list<int> items)
{
	if(items.empty())
		return -1;	

	int item1 = items.front();

	return item1;	
}